const thing = require("./some path");
import thingy from "some/other/path";
console.log(thingy);

// We are going to have some comments then
let usSet = "up a bunche of variables";
const like = true;
const arr = [];
const a = { object_with: 123 };

const float = 123.456;

let result = float + a.object_with;

document.window.window.window;

if (arr === []) {
}

class MyClass {
  constructor() {
    return this;
  }

  mew() {
    console.log(`mew`);
  }
}

let aClassThing = new MyClass();
aClassThing.mew();

export { result, float };
